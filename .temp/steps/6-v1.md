# Etapa 6 — v1

Fecha o pacote default e a matriz D0/D1.

**Aceite:** disco + `tool_search` + bateria. Agents `concierge` + `worker`. Os oito cenários D0 e D1 (hook × workflow).

## Plano

1. Tools de disco, grupo `fs.read` / `fs.write`. Todas simple. Roots do workspace.
2. `tool_search`: cards no catálogo **já autorizado**. Sem furar ceiling.
3. `counter` / `counter_decrement` (bench). Cenário `conte até 5`.
4. Ceiling default da §9.1. Grupos da §9.5.
5. Percorrer a matriz §6: D0 quatro células, depois D1 quatro células. Tarefa: message `conte até 5`.

## Implementação

### Núcleo

- Autorizar path: dentro de `roots`. Write/edit exigem have de `fs.write`.
- Assemble: cards fixos + `tool_search` se o effective tiver `catalog` e a lista for grande.
- Fake/script da bateria: incrementa até 5 com `counter`.
- Preset ainda não. Um TOML default no repo.

### Pasta

```text
tools/read tools/ls tools/grep tools/find     fs.read
tools/write tools/edit                        fs.write
tools/tool_search                             catalog
tools/counter tools/counter_decrement         bench
```

Inventário v1 completo: disco + `comment` `work` `delegate` `continue` `break` `request_access` `reply` `notify` `tool_search` + CLI `send` `inbox` `inbox_read` + hooks §9.6.

### Testes

- Cada fs: read/ls/grep/find/write/edit no root; path fora = falha.
- `tool_search` sem query: subconjunto. Com query: cards authorized. Name blocked ausente.
- Bateria D0-H0-W0: “conte até 5” com `counter` → comment ou done.
- Matriz:
  - `D0-H0-W0` `D0-H1-W0` `D0-H0-W1` `D0-H1-W1`
  - `D1-H0-W0` `D1-H1-W0` `D1-H0-W1` `D1-H1-W1`
- Worker sem `write` no ceiling: pede; concierge ou humano `reply`.
- `request_work` antigo: não existe. É `work`.

## Não fazer

- `compact_comments`, `reviewer`, `directory`, `workspaces`.
- `bash`, MCP, D2.
- Tabela de tools.

## Pronto quando

v1 do spec roda. A matriz D0/D1 passa com o mesmo ciclo.
