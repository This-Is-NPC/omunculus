#!/bin/sh
root="$(CDPATH= cd -- "$(dirname "$0")/.." && pwd)"
cwd="$PWD"
cd "$root" || exit 1
case "$1" in
  --config)
    exec mise exec -- mix omunculus "$@"
    ;;
  *)
    exec mise exec -- mix omunculus --config "$cwd/omunculus.toml" "$@"
    ;;
esac
