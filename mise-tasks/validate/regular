#!/usr/bin/env bash
#MISE description="Run the regular test suite"
set -euo pipefail

project_root="${MISE_PROJECT_ROOT:-$(git rev-parse --show-toplevel)}"
cd "$project_root"

exec mix test --exclude cargo --exclude sandbox
