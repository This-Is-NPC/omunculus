name = "bash"
kind = "tool"
shape = "simple"
triggers = ["model"]
description = "Runs a shell command in the workspace."
tags = ["shell"]
module = "Omunculus.Tools.Bash"

[parameters]
type = "object"
required = ["command"]

[parameters.properties.command]
type = "string"
