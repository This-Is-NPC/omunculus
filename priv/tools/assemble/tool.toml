name = "assemble"
kind = "tool"
shape = "simple"
triggers = ["harness"]
description = "Builds the assembled prompt of a run from its views."
tags = ["prompt"]
groups = ["prompt"]
views = ["prompt", "work", "comments.work", "comments.request", "comments.inbox", "request", "catalog"]
module = "Omunculus.Tools.Assemble"

[parameters]
type = "object"

[parameters.properties.text]
type = "string"

[parameters.properties.inbox_id]
type = "string"
