name = "break"
kind = "tool"
shape = "simple"
triggers = ["model"]
description = "Parks the work with a comment. The sequence does not advance."
tags = ["sequence", "work"]
groups = ["sequence"]
module = "Omunculus.Tools.Break"

[parameters]
type = "object"
required = ["body"]

[parameters.properties.body]
type = "string"
