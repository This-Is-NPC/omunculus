name = "comment"
kind = "tool"
shape = "simple"
triggers = ["cli", "model"]
description = "Escreve um comment no work, request ou inbox."
tags = ["comment", "work", "store"]
groups = ["store"]
module = "Omunculus.Tools.Comment"

[parameters]
type = "object"
required = ["body"]

[parameters.properties.body]
type = "string"

[parameters.properties.work_id]
type = "string"

[parameters.properties.request_id]
type = "string"

[parameters.properties.inbox_id]
type = "string"
