name = "continue"
kind = "tool"
shape = "simple"
triggers = ["model"]
description = "Asks for the next workflow step. The TOML decides which."
tags = ["sequence", "work"]
groups = ["sequence"]
module = "Omunculus.Tools.Continue"

[parameters]
type = "object"
