name = "counter_decrement"
kind = "tool"
shape = "simple"
triggers = ["model"]
description = "Decrements the benchmark counter and returns its value."
tags = ["bench"]
groups = ["bench"]
views = ["counter"]
module = "Omunculus.Tools.CounterDecrement"

[parameters]
type = "object"
