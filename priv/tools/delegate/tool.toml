name = "delegate"
kind = "tool"
shape = "simple"
triggers = ["model"]
description = "Creates a child work and hands it to whoever is below. The parent waits."
tags = ["sequence", "work"]
groups = ["sequence"]
module = "Omunculus.Tools.Delegate"

[parameters]
type = "object"
required = ["title", "body"]

[parameters.properties.title]
type = "string"

[parameters.properties.body]
type = "string"

[parameters.properties.workspace]
type = "string"
