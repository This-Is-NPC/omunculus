name = "directory"
kind = "tool"
shape = "simple"
views = ["paths"]
triggers = ["model"]
description = "Lists the run roots and the top-level entries of each."
tags = ["fs"]
module = "Omunculus.Tools.Directory"

[parameters]
type = "object"
