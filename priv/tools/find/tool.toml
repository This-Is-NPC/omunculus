name = "find"
kind = "tool"
shape = "simple"
views = ["paths"]
triggers = ["model"]
description = "Finds paths by pattern."
tags = ["fs"]
groups = ["fs.read"]
module = "Omunculus.Tools.Find"

[parameters]
type = "object"
required = ["pattern"]

[parameters.properties.pattern]
type = "string"

[parameters.properties.path]
type = "string"
