name = "find"
kind = "tool"
shape = "simple"
triggers = ["model"]
description = "Busca paths por padrão."
tags = ["fs"]
groups = ["fs.read"]
module = "Omunculus.Tools.Find"

[parameters]
type = "object"
required = ["pattern"]

[parameters.properties.pattern]
type = "string"

[parameters.properties.path]
type = "string"
