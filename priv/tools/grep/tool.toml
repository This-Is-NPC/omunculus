name = "grep"
kind = "tool"
shape = "simple"
views = ["paths"]
triggers = ["model"]
description = "Searches file contents for text."
tags = ["fs"]
groups = ["fs.read"]
module = "Omunculus.Tools.Grep"

[parameters]
type = "object"
required = ["pattern"]

[parameters.properties.pattern]
type = "string"

[parameters.properties.path]
type = "string"
