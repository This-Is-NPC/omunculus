name = "ls"
kind = "tool"
shape = "simple"
views = ["paths"]
triggers = ["model"]
description = "Lists a directory."
tags = ["fs"]
groups = ["fs.read"]
module = "Omunculus.Tools.Ls"

[parameters]
type = "object"

[parameters.properties.path]
type = "string"
