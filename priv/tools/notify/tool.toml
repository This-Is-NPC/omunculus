name = "notify"
kind = "tool"
shape = "simple"
triggers = ["model"]
description = "Notifies the person through the inbox. The run continues."
tags = ["store", "inbox"]
groups = ["store"]
module = "Omunculus.Tools.Notify"

[parameters]
type = "object"
required = ["body"]

[parameters.properties.body]
type = "string"

[parameters.properties.work_id]
type = "string"
