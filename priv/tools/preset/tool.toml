name = "preset"
kind = "tool"
shape = "simple"
triggers = ["cli"]
config = false
description = "Applies a preset: TOML and tool folders into the project."
tags = ["cli", "preset"]
groups = ["cli"]
module = "Omunculus.Tools.Preset"

[parameters]
type = "object"
required = ["name", "from"]

[parameters.properties.name]
type = "string"

[parameters.properties.from]
type = "string"
