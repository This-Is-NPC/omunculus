name = "prompt"
kind = "tool"
shape = "simple"
triggers = ["cli"]
description = "Prints the assembled prompt of a run."
tags = ["cli", "prompt"]
groups = ["cli"]
views = ["prompt"]
module = "Omunculus.Tools.Prompt"

[parameters]
type = "object"

[parameters.properties.run]
type = "string"
