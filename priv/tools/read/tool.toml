name = "read"
kind = "tool"
shape = "simple"
views = ["paths"]
triggers = ["model"]
description = "Reads a file."
tags = ["fs"]
groups = ["fs.read"]
module = "Omunculus.Tools.Read"

[parameters]
type = "object"
required = ["path"]

[parameters.properties.path]
type = "string"
