name = "send"
kind = "tool"
shape = "simple"
triggers = ["cli"]
description = "Delivers the message and opens the first run."
groups = ["cli"]
module = "Omunculus.Tools.Send"

[parameters]
type = "object"
required = ["message"]

[parameters.properties.message]
type = "string"
