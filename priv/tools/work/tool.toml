name = "work"
kind = "tool"
shape = "simple"
triggers = ["model"]
description = "Creates or updates the work and writes its title."
tags = ["work", "store"]
groups = ["store"]
module = "Omunculus.Tools.Work"

[parameters]
type = "object"
required = ["title"]

[parameters.properties.title]
type = "string"

[parameters.properties.work_id]
type = "string"

[parameters.properties.parent_id]
type = "string"

[parameters.properties.workspace]
type = "string"
